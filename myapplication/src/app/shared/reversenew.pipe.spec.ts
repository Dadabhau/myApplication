import { ReversenewPipe } from './reversenew.pipe';

describe('ReversenewPipe', () => {
  it('create an instance', () => {
    const pipe = new ReversenewPipe();
    expect(pipe).toBeTruthy();
  });
});
